package com.mrk.apps.redis.stream.config;

//@Configuration
public class StreamConfig {

//    RedisConnectionFactory connectionFactory = new
//    StreamListener<String, MapRecord<String, String, String>> streamListener = …
//
//    StreamMessageListenerContainer.StreamMessageListenerContainerOptions<String, MapRecord<String, String, String>> containerOptions = StreamMessageListenerContainerOptions
//            .builder().pollTimeout(Duration.ofMillis(100)).build();
//
//    StreamMessageListenerContainer<String, MapRecord<String, String, String>> container = StreamMessageListenerContainer.create(connectionFactory,
//            containerOptions);
//
//    Subscription subscription = container.receive(StreamOffset.fromStart("my-stream"), streamListener);
}
